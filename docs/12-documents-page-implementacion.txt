╔══════════════════════════════════════════════════════════════════╗
║   DOCUMENTS PAGE — ESTADO DE IMPLEMENTACIÓN                    ║
║   Página "Documentos": componentes, APIs, bugs                 ║
╚══════════════════════════════════════════════════════════════════╝

   1. ESTRUCTURA (DocumentsPage.tsx)
      Layout responsive, 4 secciones: Header, SourceStrip, Table, OneDriveDialog
      Ruta: #documentos → GeoNexusWorkspace.tsx:34 → DocumentsPage
      Archivos:
        src/features/workspace/documents/DocumentsPage.tsx (503 líneas)
        src/features/workspace/documents/documents-data.ts
        src/features/workspace/documents/DocumentAssetIcon.tsx

   2. DocumentHeader
      Título "Documentos y fuentes de conocimiento" con subtítulo
      3 botones de acción:
        "Subir documento" — input type=file SIN handler
        "Elegir carpeta" — input vacío SIN handler
        "Conectar OneDrive" — abre OneDriveDialog (UI only)
      3 métricas: Archivos, Chunks IA, Fuentes activas (= 3 hardcoded)

   3. SourceStrip
      4 fuentes conectables en grid 2x2:
        OneDrive         → botón "Conectar" → OneDriveDialog
        Carpeta Windows  → botón "Elegir" → SIN handler
        Subir archivos   → botón "Subir" → SIN handler
        URL/SharePoint   → botón "Agregar URL" → SIN handler
      Botón refresh (solo visual, no recarga fuentes reales)

   4. DocumentTable
      Lista de assets documentales desde listDataAssets()
      Filtro por kind: "document", "word", "excel", "other"
      Cada fila: icono, nombre, tipo, status, chunks, botón Indexar/Reindexar
      Loading: texto "Cargando biblioteca documental..." con spinner
      Empty state: "No hay documentos registrados."
      Error: alert() en catch (debería ser toast)

   5. OneDriveDialog
      UI completa con formulario de conexión OneDrive
      Botones "Probar acceso", "Cancelar", "Guardar fuente"
      NINGÚN botón tiene backend — solo UI decorativa

   6. Backend existente que se puede conectar
      register_local_connector — registrar carpeta como fuente
      sync_local_connector — escanear carpeta, crear assets
      cache_connector_file — cachear archivo + crear asset en BD
      index_document — pipeline: extract → chunk → embed → grafo
      rebuild_knowledge_graph — reconstruir grafo desde assets
      listDataAssets — ya conectado (camelCase fix aplicado)

   7. Lo que NO existe
      tauri-plugin-dialog — necesario para file picker nativo
      upload_document — command para subir archivo manual
      OAuth OneDrive — Fase 5 completa, nada implementado
      Conteo real de fuentes — no consulta connector_configs

   8. BUGS IDENTIFICADOS
      B1 ❌ Botón "Subir documento" — input file sin handler de subida
      B2 ❌ Botón "Elegir carpeta" — no abre file picker nativo
      B3 ❌ SourceStrip — 3 de 4 botones sin onClick
      B4 ❌ OneDriveDialog — formulario UI sin backend
      B5 ❌ "Fuentes activas = 3" hardcoded
      B6 ❌ Error con alert() en vez de toast
      B7 ❌ Sin skeleton loading (solo texto)
      B8 ❌ Sin debounce/search/sort/paginación
      B9 ⚠ invokeOrFallback traga errores silenciosamente

   9. PENDIENTE (priorizado)
      P1 🔴 Botón "Elegir carpeta" → register_local_connector + sync
         (requiere tauri-plugin-dialog o input manual de ruta)
      P2 🔴 Botón "Subir documento" → cache_connector_file + index_document
      P3 🟡 SourceStrip botones funcionales (Elegir, Subir, Agregar URL)
      P4 🟡 Reemplazar alert() con toast
      P5 🟡 Skeleton loading en tabla
      P6 🟢 Fuentes activas desde backend (COUNT connector_configs)
      P7 🟢 Debounce/search/sort/paginación en tabla
      P8 ⚪ OneDriveDialog conectado a backend (Fase 5, grande)

   10. ARCHIVOS
        Frontend:
          DocumentsPage.tsx — página completa (503 líneas)
          documents-data.ts — fuentes estáticas, constantes
          DocumentAssetIcon.tsx — iconos por tipo/source

        Backend relacionado:
          commands/connector.rs — register, sync, cache connector
          commands/document.rs — index_document, rebuild graph
          commands/data.rs — list_data_assets
          data_repo.rs — seed_demo_data, upsert, queries
          connector_repo.rs — CRUD conectores
